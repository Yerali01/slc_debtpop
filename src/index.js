import React from "react";
import ReactDOM from "react-dom/client";
import DeptPop from "./DeptPop";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <DeptPop />
  </React.StrictMode>
);
